# 📊 Saved Challenges Architecture

## Before (Wrong ❌)

```
┌─────────────────────────────────────┐
│        challenges Table             │
├─────────────────────────────────────┤
│ id          │ title        │ issaved│
├─────────────────────────────────────┤
│ uuid-1      │ Challenge A  │ TRUE   │ ← GLOBAL!
│ uuid-2      │ Challenge B  │ FALSE  │ ← GLOBAL!
└─────────────────────────────────────┘

Problem: When User A saves Challenge A, User B also sees it as saved!
```

---

## After (Correct ✅)

```
┌───────────────────────────────┐
│      challenges Table         │
├───────────────────────────────┤
│ id      │ title               │
├───────────────────────────────┤
│ uuid-1  │ Challenge A         │
│ uuid-2  │ Challenge B         │
└───────────────────────────────┘
           ↓ no issaved!


┌─────────────────────────────────────────────────────────────┐
│                    profiles Table                           │
├─────────────────────────────────────────────────────────────┤
│ id         │ full_name  │ saved_challenges                  │
├─────────────────────────────────────────────────────────────┤
│ user-A-id  │ Alice      │ [{"challenge_id": "uuid-1", ...}] │ ← Alice's saves
│ user-B-id  │ Bob        │ [{"challenge_id": "uuid-2", ...}] │ ← Bob's saves
└─────────────────────────────────────────────────────────────┘

Result: Each user has their own private saved list!
```

---

## Data Flow

### 1. User Clicks Save Button

```
┌─────────────┐
│  User Clicks│
│  Heart Icon │
└──────┬──────┘
       │
       ↓
┌──────────────────────────────┐
│ toggleChallengeSave(id)      │
│ (Frontend Function)          │
└──────┬───────────────────────┘
       │
       ↓
┌──────────────────────────────┐
│ supabase.rpc(                │
│   'save_challenge',          │
│   { user_id, challenge_id }  │
│ )                            │
└──────┬───────────────────────┘
       │
       ↓
┌──────────────────────────────────────┐
│ Database Function (PostgreSQL)      │
│ public.save_challenge()              │
│                                      │
│ 1. Get current saved_challenges      │
│ 2. Add new entry with timestamp      │
│ 3. Update profiles.saved_challenges  │
└──────┬───────────────────────────────┘
       │
       ↓
┌──────────────────────────────────────┐
│ profiles.saved_challenges UPDATED    │
│                                      │
│ [                                    │
│   {                                  │
│     "challenge_id": "uuid-1",        │
│     "saved_at": "2025-10-10T..."     │
│   }                                  │
│ ]                                    │
└──────┬───────────────────────────────┘
       │
       ↓
┌──────────────────────────────┐
│ Frontend Updates UI          │
│ ❤️ Heart icon turns red       │
└──────────────────────────────┘
```

---

## Database Functions

```sql
┌────────────────────────────────────────────────────────┐
│ Function: is_challenge_saved(user_id, challenge_id)   │
├────────────────────────────────────────────────────────┤
│ Input:  user_id (uuid), challenge_id (uuid)           │
│ Output: boolean (true/false)                          │
│ Purpose: Check if challenge is in user's saved list   │
└────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────┐
│ Function: save_challenge(user_id, challenge_id)       │
├────────────────────────────────────────────────────────┤
│ Input:  user_id (uuid), challenge_id (uuid)           │
│ Output: jsonb { success, saved_challenges }           │
│ Purpose: Add challenge to user's saved list           │
│ Action: INSERT into saved_challenges array            │
└────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────┐
│ Function: unsave_challenge(user_id, challenge_id)     │
├────────────────────────────────────────────────────────┤
│ Input:  user_id (uuid), challenge_id (uuid)           │
│ Output: jsonb { success, saved_challenges }           │
│ Purpose: Remove challenge from user's saved list      │
│ Action: REMOVE from saved_challenges array            │
└────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────┐
│ Function: get_saved_challenges(user_id)               │
├────────────────────────────────────────────────────────┤
│ Input:  user_id (uuid)                                │
│ Output: TABLE (challenge_id, saved_at, title, ...)    │
│ Purpose: Get all saved challenges with full details   │
│ Action: JOIN profiles.saved_challenges with challenges│
└────────────────────────────────────────────────────────┘
```

---

## Frontend Components

```
src/
├── lib/
│   ├── savedChallenges.ts          ← Helper functions
│   └── index.ts                    ← Export them
│
└── components/
    ├── SaveButton.tsx              ← Heart icon
    ├── ChallengeCard.tsx           ← Card with save button
    └── SavedChallengesPage.tsx     ← List of saved
```

---

## Example: Multi-User Scenario

```
User Alice (ID: alice-123)
┌───────────────────────────────┐
│ Saved Challenges:             │
│ • Challenge A (uuid-1)        │
│ • Challenge C (uuid-3)        │
└───────────────────────────────┘

User Bob (ID: bob-456)
┌───────────────────────────────┐
│ Saved Challenges:             │
│ • Challenge B (uuid-2)        │
└───────────────────────────────┘

User Charlie (ID: charlie-789)
┌───────────────────────────────┐
│ Saved Challenges:             │
│ • Challenge A (uuid-1)        │
│ • Challenge B (uuid-2)        │
│ • Challenge D (uuid-4)        │
└───────────────────────────────┘

Result: Each user sees different saved states!
```

---

## Security

```
Row Level Security (RLS) on profiles:
┌─────────────────────────────────────────────────────┐
│ Policy: allow_authenticated_update_own_profile      │
│ ─────────────────────────────────────────────────── │
│ Users can ONLY update their OWN profile             │
│ WHERE auth.uid() = profiles.id                      │
│                                                     │
│ Result: Alice cannot modify Bob's saved_challenges  │
└─────────────────────────────────────────────────────┘

Function Security:
┌─────────────────────────────────────────────────────┐
│ SECURITY DEFINER                                    │
│ ─────────────────────────────────────────────────── │
│ Functions run with elevated privileges              │
│ But check user_id matches auth.uid() internally     │
│                                                     │
│ Result: Safe and efficient                          │
└─────────────────────────────────────────────────────┘
```

---

## Performance

```
Query Saved Challenges:
┌─────────────────────────────────────────┐
│ SELECT * FROM profiles                  │
│ WHERE id = current_user_id              │
│ ─────────────────────────────────────── │
│ Speed: O(1) - Single row lookup         │
│ Index: Primary key on id                │
└─────────────────────────────────────────┘

Check if Saved:
┌─────────────────────────────────────────┐
│ saved_challenges @> '[{"id": "..."}]'   │
│ ─────────────────────────────────────── │
│ Speed: O(log n) - JSONB index           │
│ Scalable: Works with 1000s of saves     │
└─────────────────────────────────────────┘

Get Saved with Details:
┌─────────────────────────────────────────┐
│ JOIN challenges ON id = challenge_id    │
│ ─────────────────────────────────────── │
│ Speed: O(n) where n = # of saved        │
│ Typical: 10-50 saved challenges/user    │
└─────────────────────────────────────────┘
```

---

## Migration Impact

```
BEFORE Migration:
challenges.issaved = TRUE   → All users see it as saved ❌

AFTER Migration:
profiles.saved_challenges = [
  { challenge_id: "uuid", saved_at: "..." }
]
→ Only THIS user sees it as saved ✅
```

---

## Summary

**Old System:**
- ❌ Global save state
- ❌ No privacy
- ❌ Can't track who saved what
- ❌ Can't add save metadata

**New System:**
- ✅ Per-user save state
- ✅ Private saved lists
- ✅ Track save timestamps
- ✅ Extensible (add notes, tags, etc.)
- ✅ Scalable (JSONB indexing)
- ✅ Secure (RLS policies)

---

Perfect architecture for user-specific saved challenges! 🎯
